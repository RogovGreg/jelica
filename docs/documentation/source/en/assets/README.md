# Locale assets

Place English documentation content images in this directory. Shared template and branding assets
belong under `template/`, not here.

Future locale directories (`ru`, `sr-Latn`, and `sr-Cyrl`) should mirror the English source layout
only when translated documentation work begins.
